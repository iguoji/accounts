# Agent 工作规范

## 一、沟通与决策

- 所有面向用户展示的内容，包括分析说明、进度提示和最终答复，均使用中文；代码、命令及必要的原文引用除外。
- 每次最终回复的首行标明可确认的公开模型名、版本号及知识截止日期；无法确认的项目填写“未知”，不得猜测或伪造。
- 不逐项播报工具操作或重复解释操作理由，只提供必要的进度、重要发现、阻碍及需用户决策的事项。
- 所有面向用户的说明均使用通俗、具体的表达，不默认用户具备技术背景；必要的专业术语首次出现时，结合当前事情解释其含义。
- 按清晰的逻辑顺序说明事情，交代必要的前因后果，不跳步、不堆砌概念，不让用户自行推断对象、原因或结论。
- 以容易理解为先，不为简短而省略必要信息；每句话尽量只表达一个意思，多项内容分条说明。

## 二、任务与执行

- 每次收到用户新消息时，先重新读取本文件的最新内容，再处理任务，不以记忆或此前读取的内容代替。
- 执行前先确认关键条件和主要方案，仅针对明确风险准备备用方案。
- 可安全合并的操作集中执行，避免无必要的重复读取、零散调用和盲目重试。

## 三、代码与实现



## 四、检查与验证

- 只修改完成需求所必需的文件，不额外创建测试用例、截图、图示等辅助产物，除非用户明确要求。
- 完成后简要说明改动，并如实标明未执行的验证，不将“修改完成”表述为“验证通过”。
- 如有环境搭建需求，需要列出方案和细节后，由用户明确指定才能搭建。

## 五、安全与授权



## 六、交付与维护

- 分支管理
    - `main` 只保留经用户确认可交付的内容，不直接在该分支开发功能或修复问题；发布时更新版本号和说明文件除外。
    - 开发工作在独立的功能分支上完成，分支名称采用 `feature/简短描述`。
- 提交变更
    - 每完成一项完整且可独立描述的变更后，主动提交一次，不按文件修改或保存的次数提交。
    - 提交信息采用“标题 + 多行正文”：标题概括变更，正文用通俗语言说明改了什么、为什么改，以及对使用者有什么影响。
    - 只提交当前任务涉及的内容，不夹带用户的其他修改、敏感信息或临时文件。
- 合并分支
    - 只有用户明确确认后，才可将功能分支合并到 `main`。
    - 合并前，确保工作区没有未提交的变更，并将 `main` 的最新内容同步到功能分支；如有冲突，先处理，不覆盖用户修改。
    - 使用压缩合并，将该功能分支的多次提交整理为一条记录，再合入 `main`。
- 更新版本
    - 仅当代码变更合入 `main` 后更新版本号，功能分支上的提交不单独更新版本号。
    - 版本号采用 `vX.Y.Z`，默认只递增末位 `Z`；前两位 `X`、`Y` 仅在用户明确要求时调整。
    - 版本号更新在 `main` 上完成，不在功能分支中提前递增。
- 更新说明与标记版本
    - 更新版本号时，同步更新相关 `README.md` 和已有的版本记录文件，用通俗语言说明本次变化及使用时需要注意的事项。
    - 涉及子系统时，详细说明放在对应子系统的 `README.md`；全系统的 `README.md` 只补充必要的概述，避免重复。
    - 将版本号和说明文件的修改提交后，在 `main` 的该次提交上创建同名标签 `vX.Y.Z`，明确这个版本对应哪份代码。